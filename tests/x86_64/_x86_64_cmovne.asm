cmovne rax, rax
cmovne rax, rcx
cmovne rax, rdx
cmovne rax, rbx
cmovne rax, rsp
cmovne rax, rbp
cmovne rax, rsi
cmovne rax, rdi
cmovne rax, r8
cmovne rax, r9
cmovne rax, r10
cmovne rax, r11
cmovne rax, r12
cmovne rax, r13
cmovne rax, r14
cmovne rax, r15
cmovne rcx, rax
cmovne rcx, rcx
cmovne rcx, rdx
cmovne rcx, rbx
cmovne rcx, rsp
cmovne rcx, rbp
cmovne rcx, rsi
cmovne rcx, rdi
cmovne rcx, r8
cmovne rcx, r9
cmovne rcx, r10
cmovne rcx, r11
cmovne rcx, r12
cmovne rcx, r13
cmovne rcx, r14
cmovne rcx, r15
cmovne rdx, rax
cmovne rdx, rcx
cmovne rdx, rdx
cmovne rdx, rbx
cmovne rdx, rsp
cmovne rdx, rbp
cmovne rdx, rsi
cmovne rdx, rdi
cmovne rdx, r8
cmovne rdx, r9
cmovne rdx, r10
cmovne rdx, r11
cmovne rdx, r12
cmovne rdx, r13
cmovne rdx, r14
cmovne rdx, r15
cmovne rbx, rax
cmovne rbx, rcx
cmovne rbx, rdx
cmovne rbx, rbx
cmovne rbx, rsp
cmovne rbx, rbp
cmovne rbx, rsi
cmovne rbx, rdi
cmovne rbx, r8
cmovne rbx, r9
cmovne rbx, r10
cmovne rbx, r11
cmovne rbx, r12
cmovne rbx, r13
cmovne rbx, r14
cmovne rbx, r15
cmovne rsp, rax
cmovne rsp, rcx
cmovne rsp, rdx
cmovne rsp, rbx
cmovne rsp, rsp
cmovne rsp, rbp
cmovne rsp, rsi
cmovne rsp, rdi
cmovne rsp, r8
cmovne rsp, r9
cmovne rsp, r10
cmovne rsp, r11
cmovne rsp, r12
cmovne rsp, r13
cmovne rsp, r14
cmovne rsp, r15
cmovne rbp, rax
cmovne rbp, rcx
cmovne rbp, rdx
cmovne rbp, rbx
cmovne rbp, rsp
cmovne rbp, rbp
cmovne rbp, rsi
cmovne rbp, rdi
cmovne rbp, r8
cmovne rbp, r9
cmovne rbp, r10
cmovne rbp, r11
cmovne rbp, r12
cmovne rbp, r13
cmovne rbp, r14
cmovne rbp, r15
cmovne rsi, rax
cmovne rsi, rcx
cmovne rsi, rdx
cmovne rsi, rbx
cmovne rsi, rsp
cmovne rsi, rbp
cmovne rsi, rsi
cmovne rsi, rdi
cmovne rsi, r8
cmovne rsi, r9
cmovne rsi, r10
cmovne rsi, r11
cmovne rsi, r12
cmovne rsi, r13
cmovne rsi, r14
cmovne rsi, r15
cmovne rdi, rax
cmovne rdi, rcx
cmovne rdi, rdx
cmovne rdi, rbx
cmovne rdi, rsp
cmovne rdi, rbp
cmovne rdi, rsi
cmovne rdi, rdi
cmovne rdi, r8
cmovne rdi, r9
cmovne rdi, r10
cmovne rdi, r11
cmovne rdi, r12
cmovne rdi, r13
cmovne rdi, r14
cmovne rdi, r15
cmovne r8, rax
cmovne r8, rcx
cmovne r8, rdx
cmovne r8, rbx
cmovne r8, rsp
cmovne r8, rbp
cmovne r8, rsi
cmovne r8, rdi
cmovne r8, r8
cmovne r8, r9
cmovne r8, r10
cmovne r8, r11
cmovne r8, r12
cmovne r8, r13
cmovne r8, r14
cmovne r8, r15
cmovne r9, rax
cmovne r9, rcx
cmovne r9, rdx
cmovne r9, rbx
cmovne r9, rsp
cmovne r9, rbp
cmovne r9, rsi
cmovne r9, rdi
cmovne r9, r8
cmovne r9, r9
cmovne r9, r10
cmovne r9, r11
cmovne r9, r12
cmovne r9, r13
cmovne r9, r14
cmovne r9, r15
cmovne r10, rax
cmovne r10, rcx
cmovne r10, rdx
cmovne r10, rbx
cmovne r10, rsp
cmovne r10, rbp
cmovne r10, rsi
cmovne r10, rdi
cmovne r10, r8
cmovne r10, r9
cmovne r10, r10
cmovne r10, r11
cmovne r10, r12
cmovne r10, r13
cmovne r10, r14
cmovne r10, r15
cmovne r11, rax
cmovne r11, rcx
cmovne r11, rdx
cmovne r11, rbx
cmovne r11, rsp
cmovne r11, rbp
cmovne r11, rsi
cmovne r11, rdi
cmovne r11, r8
cmovne r11, r9
cmovne r11, r10
cmovne r11, r11
cmovne r11, r12
cmovne r11, r13
cmovne r11, r14
cmovne r11, r15
cmovne r12, rax
cmovne r12, rcx
cmovne r12, rdx
cmovne r12, rbx
cmovne r12, rsp
cmovne r12, rbp
cmovne r12, rsi
cmovne r12, rdi
cmovne r12, r8
cmovne r12, r9
cmovne r12, r10
cmovne r12, r11
cmovne r12, r12
cmovne r12, r13
cmovne r12, r14
cmovne r12, r15
cmovne r13, rax
cmovne r13, rcx
cmovne r13, rdx
cmovne r13, rbx
cmovne r13, rsp
cmovne r13, rbp
cmovne r13, rsi
cmovne r13, rdi
cmovne r13, r8
cmovne r13, r9
cmovne r13, r10
cmovne r13, r11
cmovne r13, r12
cmovne r13, r13
cmovne r13, r14
cmovne r13, r15
cmovne r14, rax
cmovne r14, rcx
cmovne r14, rdx
cmovne r14, rbx
cmovne r14, rsp
cmovne r14, rbp
cmovne r14, rsi
cmovne r14, rdi
cmovne r14, r8
cmovne r14, r9
cmovne r14, r10
cmovne r14, r11
cmovne r14, r12
cmovne r14, r13
cmovne r14, r14
cmovne r14, r15
cmovne r15, rax
cmovne r15, rcx
cmovne r15, rdx
cmovne r15, rbx
cmovne r15, rsp
cmovne r15, rbp
cmovne r15, rsi
cmovne r15, rdi
cmovne r15, r8
cmovne r15, r9
cmovne r15, r10
cmovne r15, r11
cmovne r15, r12
cmovne r15, r13
cmovne r15, r14
cmovne r15, r15
